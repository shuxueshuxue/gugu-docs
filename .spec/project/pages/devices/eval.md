---
scenarios:
  - name: 设备页可从导航访问
    description: >
      启动本地 Mintlify 站点，以读者身份从「用法」导航进入「设备」，检查页面内容与层级。
    expected: >
      「设备」出现在「用法」导航中，/devices 页面正常渲染；正文能找到设备自动登记、列表字段、在线判定、
      退出后的状态、本地与云端选择、文件位置和当前设置入口，且不把 SSH 目标或工作目录选择写成可用功能。
    tags: [frontend-e2e]
---

通过本地 Mintlify 预览检查读者实际看到的页面。构建检查只作为辅助证据，不能代替网页访问。
